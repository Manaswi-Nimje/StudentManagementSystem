# Copy to .env and adjust before running `docker compose up`.
ORACLE_USERNAME=studentapp
ORACLE_PASSWORD=student123

# Signs login tokens — use a long random string in any real deployment.
# Generate one with: openssl rand -base64 48
JWT_SECRET=dev-only-secret-change-me-please-this-should-be-at-least-32-bytes-long
